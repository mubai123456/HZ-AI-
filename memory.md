## 2026-03-25 应用配置与生产稳定性长期规则补充

- `JWT_SECRET` 的强制安全校验只允许跟 `APP_ENV=production` 绑定，不能再直接用 `NODE_ENV=production` 作为“真实生产环境”的判定。
- RunningHub 新建应用导入链路里，select 字段的真实来源优先看 `fieldData`，只有在缺少元数据时才允许退回字段名启发式推断。
- RunningHub `fieldData` 目前至少要兼容两种格式：
  - `[[values], { default }]`
  - `[{ name, index, description, fastIndex }, ...]`
- 应用节点编辑器里的显示开关，长期语义固定为“默认前台显示，管理员可取消显示”；底层继续存 `hidden`，不要为了 UI 语义去改库结构。
- 单应用页左栏的 textarea 规则固定为：
  - 第一个 textarea 作为主提示词区域
  - 其余可见 textarea 统一放进“补充文本参数”
  - 隐藏 textarea 不对用户展示，但仍允许沿用默认值参与提交
- 飞书补同步批处理默认应使用稳定排序；如果任务天然带 `siteTaskNo`，优先按 `siteTaskNo` 排序，避免同秒创建记录导致测试和实际执行顺序漂移。
- 本地常用演示应用 `2-0` 需要和当前正确 schema 保持一致；如后续重置环境，seed 必须能把它修回 dropdown 版本，避免旧坏数据反复回流。

## 2026-03-25 Supabase / Postgres 升级分支长期规则

- 当前升级主线固定为 `codex/supabase-upgrade-from-1-13`，稳定回退基线固定为 `codex/restore-1-11-case-results`。
- 这条升级线的本地开发默认数据库已经切到 `Postgres`，不要再沿用 `1.11` 的 SQLite `.env.local` 覆盖。
- Prisma 运行时固定使用 `@prisma/adapter-pg`；本分支后续不要把 SQLite adapter 再混回主运行时。
- 迁移相关环境变量约定固定为：
  - `DATABASE_URL`：应用运行时读写
  - `DIRECT_URL`：迁移和管理脚本
  - `TEST_DATABASE_URL`：Vitest 专用测试库
- Vercel 部署预检现在默认要求：
  - `DATABASE_URL` 为 Postgres
  - `DIRECT_URL` 已配置
  - 七牛 Kodo / S3 兼容对象存储参数齐全
  - 正式环境必须有 `RUNNINGHUB_WEBHOOK_SECRET`
- 当前升级线的文件存储策略固定为：
  - 素材、视频、任务输入参考图走自有对象存储
  - RunningHub 输出图一期默认只保存 `providerResultUrl`
  - 如需切回自有持久化，使用 `TASK_OUTPUT_STORAGE_MODE=object_storage`
- 正式环境禁止把七牛测试域名填进 `S3_PUBLIC_BASE_URL`；production 必须使用自定义 HTTPS 域名。
- 项目内 Vercel 部署脚本必须使用 `node -- scripts/deploy-vercel.mjs ...`，避免和 Node 自带 `--env-file` 参数解析冲突。
- Prisma 迁移链路必须优先使用 `DIRECT_URL`；应用运行时继续使用 `DATABASE_URL`。
- 本地对象存储 fallback 只允许用于本机开发；`Vercel preview / production` 不再允许 SQLite 或 local-only 存储通过预检。
- 跨数据库后，测试不要再假设 SQLite 的隐式排序；需要顺序稳定时必须显式排序，或者把断言写成顺序无关。

## 2026-03-25 1.11 恢复版长期规则

- 当前恢复验证分支固定为 `codex/restore-1-11-case-results`，代码基线是 `0912160`。
- `1.11` 本地构建与启动验证默认应显式使用 `DATABASE_URL=file:./dev.db`，避免被本机其他 `.env` 数据库配置串扰。
- `1.11` 本地开发模式优先通过 `.env.local` 覆盖仓库 `.env`；原因是仓库 `.env` 保留了后续版本的 Postgres 配置，而 `1.11` 仍是 SQLite 适配器。
- 回退旧库后不能只信任 JWT 本身；`getCurrentSession` 必须再回数据库确认用户存在且有效，否则旧 cookie 会把不存在的 `user.id` 带进写库链路，触发外键错误。
- RunningHub 失败任务必须优先提取并保存 `failedReason.exception_message`；只保存泛化 `errorMessage` 会丢掉像“显存不足”这种真正可执行的排障信息。
- `1.11` 恢复版的 `/apps/[code]` 中间主区固定采用统一“结果区”容器，内部通过“结果 / 案例”双标签切换，不再把案例展示单独插在 `WorkbenchLayout` 外层。
- `/tasks/[id]` 继续保持独立任务详情页职责，不把案例展示并入任务详情页。
- `AppShowcaseGallery` 需要同时支持独立展示和嵌入中间区两种用法，后续再改案例展示时不要只考虑单一场景。

## 2026-03-24 乱码与交互可读性规则补充

- 用户可见页面出现乱码时，优先先查共享文案源和高频模板组件，不要只在单页打补丁。
- 全局链接颜色不得默认强制覆盖所有 `a`，否则会污染“视觉上是按钮”的链接和左侧导航激活态；默认应继承组件语义颜色。
- 单应用页底部提交卡必须同时承载主动作和费用确认，`预计费用` 优先放在提交动作附近，而不是漂在左栏其他位置。
- 管理员任务中心列表如果已有输出图，默认应在列表内提供首张结果缩略预览；放大预览与详情跳转分离，不把两种行为绑成同一点击区域。
- 单应用页左栏的最终提交入口默认只保留一个主按钮；`清空` 这类辅助动作应上移到相关输入区标题右侧，不再占据底部主操作区。

## 2026-03-23 参考图标签表达规则补充

- 单应用页参考图槽位需要保留“主参考图 / 辅助图”语义，但标签应放在槽位外部，不再覆盖到图片上。
- 主参考图的强调方式优先使用更醒目的标签样式，不重新引入覆盖层、清空按钮或长文案。

## 2026-03-23 单应用页参考图槽位长期规则

- 单应用页左侧参考图输入区采用固定三列缩略图槽位，槽位内不再展示“主参考图 / 辅助图 / 点击替换 / 清空”等可见文案按钮。
- 已上传的参考图必须保持整块可点击，以支持用户直接替换图片；不要求先清空再重新上传。
- 参考图槽位的主次关系只通过轻量样式表达，不再通过额外文字层或叠加操作按钮表达。

## 2026-03-23 单应用模板与任务流长期规则

- `/apps/[code]` 单应用页固定使用「全局顶栏承载返回入口与应用名 + 三栏主体」结构，不再在页面内容区重复渲染 `Workbench` 头卡或 `Workspace` 说明卡。
- 单应用页右侧任务栏的数据源固定为“当前登录用户的全站任务”，不是当前应用任务；默认上限仍是最近 100 条，后续如需扩容优先做分页或虚拟列表。
- 单应用页允许跨应用查看历史任务结果，但“一键同款”只能作用于当前应用任务；跨应用任务禁止把输入参数直接写回当前表单。
- `Task.siteTaskNo` 是全站唯一、面向站内展示的任务编号，格式固定为 `WB-` + 6 位递增数字；提交任务时必须先分配该编号，第三方失败任务也要保留编号。
- `taskNo` 继续承担第三方任务 ID 语义；前端展示时优先使用 `siteTaskNo`，第三方任务 ID 作为次级信息折叠显示。
- `providerTaskId` 的可见性规则固定为：管理员可见，任务所有者也可见；普通用户不能因此获得其他用户任务数据。

# 项目长期记忆

## 产品与技术

- 项目是内部 AI 应用工作台，核心链路围绕应用中心、任务中心、素材体系、提示词模板、飞书同步和后台运营管理。
- 技术栈固定为 `Next.js App Router + React + Prisma + SQLite + Bun`。

## 长期规则

- 每轮任务结束后都要同步维护 `README.md`、`log.md`、`memory.md` 和 `tasks/todo.md`。
- 先修根因，再做界面；不能只改 UI 而不校对数据层、接口参数和状态返回。
- 仓库经常处于脏工作区，默认只处理当前任务相关文件，不回滚无关改动。
- 网站治理相关工作必须先写文档，再进入实现。
- 网站治理实施顺序固定为：先模板层，后页面层。
- 配置中心相关工作必须先定义配置边界，再实现后台配置能力。
- 未经计划确认，不得借 UI 或配置治理名义触碰核心业务链路。
- 用户侧导航与管理侧导航必须分层组织，不能再以扁平数组直接拼接。
- 新页面或重构页面必须先归类到既定模板类型，再进入实现。
- 敏感密钥继续采用 env 优先策略，后台默认只展示配置状态，不提供普通明文编辑。
- 全局壳层只负责品牌、导航和当前位置，不再承担页面级主标题。
- 列表页和浏览页的主标题必须留在页面模板层，客户端内容组件默认以嵌入模式接入。
- 后台删除类操作优先复用共享确认对话框，不再继续扩散浏览器原生 `window.confirm`。
- 单条操作与批量操作必须分开建模，不能让单条按钮隐式依赖当前勾选集。
- 后台输入型批量操作优先复用共享表单对话框，不再继续扩散浏览器原生 `window.prompt`。
- 后台多字段单条编辑动作也应优先使用共享表单对话框，避免再回退到串行 prompt 流程。
- 标签管理器、素材管理器和编辑器内嵌管理弹层也要遵守同一套确认/表单对话框规则，不能因为“在弹层里”就退回原生弹窗。

## 素材后台规则

- 后台素材管理页面依赖 `src/components/admin-materials-client.tsx`，这是关键组件。
- 素材后台工作流固定为“列表为主 + 上传弹窗 + 批量处理”，不再恢复旧的常驻大上传区。
- 素材后台的批量工具栏默认隐藏，只在用户勾选素材后出现，避免首屏被运营动作打断。
- 单条素材操作入口固定为“查看”，并由右侧详情抽屉承接预览、编辑、上下架和删除。
- 后台素材管理默认按电脑端运营场景设计，素材列表、领取日志和上传管理优先使用横向表格/工作台，不回退为竖向卡片式后台。
- 上传优先使用逐文件提交协议，避免一次性大 `FormData` 导致整批失败。
- 素材批量操作统一走 `/api/internal/admin/materials/bulk`，尽量与单条编辑共用同一套数据层规则。
- 标签删除的语义固定为“删除标签并解除素材关联”，不是删除素材。
- 标签重命名必须全局生效，并同步影响历史素材列表和筛选结果。
- 批量重置回公海只对当前已领取素材生效；不适用项返回跳过结果，而不是整批报错。
- 批量上传弹窗固定采用“左侧上传设置 + 右侧标签库 + 下方上传队列表格”的横向工作台结构；标签管理默认使用表格，不再使用竖向标签卡堆叠。

## 素材大厅规则

- 素材大厅优先按移动端体验设计，手机用户是主使用场景。
- 素材大厅的主预览路径已经改为“卡片内 inline video”，完整预览弹层只做次级确认，不再回到“先点预览再看片”的旧链路。
- 桌面端以悬停播放为主，手机端以主视区自动静音播放为主；声音默认静音，并按整个素材大厅统一建模，用户可手动打开也可再次关闭。
- 素材大厅 inline 小视频必须优先保证完整画面可见，使用不裁切的适配策略，不为了铺满卡片牺牲内容主体。
- 手机端浏览素材时，向下滚动后应优先把屏幕空间留给内容，筛选区默认自动收起，不要持续悬停占位。
- 电脑端素材大厅的封面区本身就是主预览入口，点击卡片封面应直接打开完整预览；完整预览遮罩点击空白区即可关闭。
- 桌面端卡片默认优先显示视频首帧，不继续依赖 `poster` 把首帧盖住。
- 素材大厅完整预览弹窗的关闭规则固定为：除视频本体、播放器控件、正文内容和按钮外，其余留白点击都关闭，包括桌面端黑色留白和右侧信息栏空白。
- 列表页负责“发现”，预览层负责“确认”，领取/下载是确认后的动作。
- 手机端默认采用单列大卡流，目标是一屏展示 2 个完整卡片 + 第 3 个露头。
- 桌面端采用高密度网格，宽屏目标为 5 列、首屏约 10 个视频。
- 大厅卡片默认只展示标题、标签、时长、上传日期，不默认堆叠批次号、文件名、尺寸或长描述。
- 搜索和标签筛选继续使用 URL 参数 `q` 与 `tag`，保证刷新和分享后的状态一致。
- 预览文件链路在本地私有存储下必须严格校验物理文件存在性，文件缺失时返回 404，不允许再让 `stat()` 直接抛成 500。
# 后台列表重构规则补充

- 后台列表统一优先走“列表页完成 CRUD + 批量操作”的模式，不再把核心管理动作都压到详情页。
- 批量接口统一使用 `POST /bulk`，请求体固定为 `{ ids, action }`，返回体固定为 `{ results, summary }`。
- 单条和批量操作要共用同一套数据层规则，尤其是用户删除/归档这类带业务语义的动作，不能前端和后端各写一套。
- 统一复用：
  - `src/components/use-list-selection.ts`
  - `src/components/admin-bulk-toolbar.tsx`
- 当前已经接入统一批量模式的后台列表：
  - 应用列表
  - 用户列表
  - 分类列表
  - Banner 列表
# 2026-03-23 补充记忆

- 提示词模板后台列表现在以 `src/components/admin-prompt-templates-client.tsx` 为主入口，列表内已接回批量启停、批量改分类、批量改标签、批量改范围、批量删除。
- 提示词标签和模板分类的后台管理器已经并入提示词模板页，不再只依赖零散弹窗或旧入口。
- 应用标签的统一管理入口位于 `src/components/app-form-editor.tsx`，保留快速新建，同时新增完整管理弹层。
- 本项目的后台列表重构验证仍以 `bun run build` 为主；`bun test` 当前受仓库既有 Bun/Prisma/Vitest 兼容问题影响，不能直接作为单轮改动成败判断。
- 未经用户确认，不删除用户已上传的视频、已有任务或其他业务数据；若排障需要触碰这类数据，必须先征求确认
- 素材大厅的生产态真机回归要以 `next start` 为准；`next dev` 下的 HMR 噪音不能直接当成业务逻辑失效
- 素材大厅移动端首屏密度要用实测数据校验：在 `390x844` 视口下，至少满足 2 张完整卡片
- `src/components` 范围内禁止新增浏览器原生 `window.confirm / window.prompt`；后台交互统一复用共享 `ConfirmDialog` 与 `FormDialog`
- `AppFormEditor` 必须支持嵌入模式，页面级标题和说明由 Editor 模板承载，编辑器组件只负责内容与操作
- `Settings` 与 `Detail Inspection` 页面优先复用统一 `PageTemplate` 落地，再决定是否需要拆分更细的模板壳层
- Browse 和 Profile 这类页面即使先接入 `PageTemplate`，后续也要继续清理客户端内部的重复页头，避免模板层和内容层同时声明主标题
- `MaterialHallClient` 与 `ProfileClient` 已进入 `embedded` 约束范围；后续同类客户端组件接入模板时也必须支持收回页面级页头
# 2026-03-23 补充规则

- 站点品牌、工作区标签、导航命名统一由 `src/lib/settings.ts + src/lib/site-config.ts` 提供，后续不得再把站点名和导航名写死在壳层或页面里
- `RunningHub / Feishu` 的非敏感基础参数统一走 `IntegrationSettings`，敏感密钥继续 `env` 优先，只能在“密钥状态”页做只读展示
- 配置中心正式分为 `站点设置 / 集成设置 / 密钥状态` 三块，后续新增可配置项必须先判断归属，再决定是否落库
- Phase 4 之后，任何“治理完成”“回归通过”类结论都必须附带新鲜证据：至少包含一轮新的构建、相关定向测试和真实浏览器回归
- 浏览器回归中出现的控制台或网络异常，必须当轮修复，或以正式文档记录为“已知残余风险”；不能只留在聊天里
- 新页面接入前必须先选定模板类型，新配置项接入前必须先判定属于 `站点设置 / 集成设置 / 仅 env`
- 移动端回归至少保留一次 `390 x 844` 的壳层导航验证，确保抽屉或折叠导航可打开、可关闭
- Browse 型列表页默认不得在首屏为每条素材主动预加载重资源预览地址；预览资源应在用户明确进入预览动作后再加载
- 素材预览文件缺失时，列表首屏不应主动打控制台 `404`；若用户手动打开预览失败，前端必须给出明确降级提示
## 2026-03-23 Workbench 规则补充

- `/apps/[code]` 工作台固定采用 `左栏发起 / 中栏结果 / 右栏队列` 的职责分工，后续优化不得再把三栏做成同权信息面板。
- `src/components/workbench-layout.tsx` 是 Workbench 唯一三栏骨架入口；左右面板折叠、页头控制和响应式退化优先在模板层处理，不把折叠心智下放到子面板。
- Workbench 当前选中任务需要和 URL 保持同步，刷新页面后应尽量恢复到相同任务；如果任务已不存在，再回退到最新任务。
- 左栏表单优先级固定为：参考图、Prompt、关键参数、提交反馈、提交按钮；低频参数默认收进折叠区。
- 中栏结果区优先表达“任务状态 + 结果本身 + 关键操作”，下载、查看大图、复制任务 ID 等动作不要再漂浮成图片角落悬浮按钮。
- 右栏在单应用场景下只做任务队列，不重复展示应用名；卡片主信息优先级是缩略图/状态、短标识、时间/耗时，其次才是复制任务号等次级动作。
- Workbench 的真实浏览器回归优先使用生产态 `next start`，不要用带 HMR 噪音的开发态页面直接下最终结论。
## 2026-03-23 单应用模板长期规则

- 单应用页优先服务“连续提交、连续看结果、继续微调”，不再把自己做成任务历史浏览中心。
- `/apps/[code]` 默认结构固定为「左侧输入工作台 + 中间结果主画布 + 右侧最新任务卡」，右侧只保留 1 个最近任务视角。
- 单应用页中的输入草稿按应用维度自动保留；提交成功后不清空，必须提供显式“清空草稿”入口。
- 输入区的低频参数默认折叠，提交入口要始终贴近左侧底部可见区域，避免用户滚动到底才能提交。
- 结果区优先级固定为：任务状态、主结果、核心动作、折叠输入摘要；输入摘要默认折叠，避免首屏被重复信息占满。
- `?task=` 在单应用页里只作为首次进入时的可选定位，不再作为运行时频繁切换结果区的主控制源。
- 单应用页内的“再次生成 / 做同款”优先复用现有任务输入与提交接口，不新增后端 schema。
- 单应用页草稿恢复不能只依赖首次 state 初始化；必须在客户端挂载后再做一次草稿回填，兜住 SSR 和路由离开再返回时的参数恢复。
- 任何 SSR 的 Client Component 都不能在初始 render 里直接用 `window` / `localStorage` 改变 DOM 结构；首帧必须和服务端一致，客户端差异统一放到挂载后的 effect 里。
## 2026-03-23 应用配置长期规则补充

- 应用删除必须优先走统一级联删除服务，不能在路由层或批量接口里直接硬删 `App`，否则会被 `Task` 等关联数据阻塞。
- 任何以 `app.code` 拼接的管理页链接、前台访问链接和 API 请求路径，都必须统一做 `encodeURIComponent`，兼容历史中文 code 和特殊字符 code。
- 新建应用创建成功后的跳转必须以服务端实际返回的 `app.code` 为准，不能继续使用本地输入框里的 `code` 作为详情页路由来源。
- 新建应用的同步配置默认带入站点级飞书字段映射，避免每个应用重复手填；但保存后仍写入应用自己的 `syncMappingJson`。
- 默认应用 `code` 生成策略优先使用 ASCII slug；名称无法生成时回退到 `providerAppId`，再回退到时间戳 code。
## 2026-03-23 单应用页精简规则补充
- 单应用页 Workbench 不再保留顶部折叠/抽屉控制条；桌面端固定三栏，移动端固定为“输入区 -> 结果区 -> 全站任务”的纵向堆叠。
- 单应用页左栏不再展示“输入工作台”这类说明卡，输入区优先只保留真正参与提交的内容和底部提交动作。
- 单应用页结果区不再重复展示输入摘要、Prompt 摘要、关键参数摘要和参考图链接；重复输入信息优先从结果区移除。
- 单应用页右栏全站任务流固定采用方案 C 的表格式卡片，字段顺序优先为：站内编号、状态、应用名、时间、耗时、结果摘要、第三方任务 ID、操作按钮。
- 第三方任务 ID 默认折叠；跨应用任务默认只允许查看结果，不允许一键同款写回当前表单。
## 2026-03-23 单应用页第三轮压缩规则补充
- 单应用页左栏的参考图区固定按三列等宽展示，不再恢复成“1 张主图大卡 + 2 张辅图小卡”。
- 参考图区的主次关系只允许用轻量样式表达，不再通过尺寸差异表达主图优先级。
- 单应用页右栏全站任务流优先做高密度信息列表，不再回到大卡片 + 多按钮 + 多段文案的展示方式。
- 右栏任务列表默认只保留缩略图、站内编号、状态、耗时、应用名、时间、第三方 ID；摘要和列表项内操作按钮默认移除。
- “一键同款”这类动作优先放在中间结果区，不再反向占据右栏任务列表密度。
## 2026-03-23 链接按钮规则补充
- 结果区这类“视觉上是按钮、语义上是链接”的入口，不能只依赖 Tailwind 的 `text-white`，必须考虑全局 `a` 样式覆盖风险。
- 后续凡是主操作链接按钮，优先使用明确的按钮化链接样式，必要时显式锁定文字颜色，避免蓝底白字被全局链接色覆盖。
## 2026-03-23 任务ID与任务中心长期规则补充

- `siteTaskNo` 固定表示站内编号，面向站内列表与详情页主识别；`providerTaskId || taskNo` 固定表示“任务ID”，不再对用户使用“第三方 ID”命名。
- 单应用页桌面端右侧“全站任务”面板默认收起，使用贴右中部的轻量开关控制；移动端不复用这套收起逻辑。
- 管理员任务中心默认支持批量勾选、批量重试同步和批量删除；批量删除必须级联清理任务相关素材、同步日志和积分记录。
- 飞书同步字段映射的底层键继续使用 `taskNo`，但后台 UI、说明文案和默认示例统一对外称为“任务ID”。
## 2026-03-24 单应用结果区多图规则

- 单应用结果区对 `TaskRecord.outputAssets` 的默认解释固定为“结果集合”，不是“主图 + 附件”。
- 多图结果默认采用“主图 + 缩略条”模式，不回退为瀑布流或整页网格。
- 结果缩略图组件必须兼容外链 URL 与站内相对路径 `/assets/...`，因为任务成功后结果图会优先落到本地公开目录。
- 结果操作区在多图场景下默认只承诺逐张查看、逐张下载；“下载全部”属于后续能力，未实现前不要伪装成可用按钮。
## 2026-03-24 任务结果多图下载规则

- 多图任务结果默认同时提供三种动作：`下载当前图片`、`直接下载多张`、`下载全部 ZIP`。
- 任务结果下载必须统一经过任务下载接口做权限校验和文件名控制，前端不要再直接用裸 `asset.url` 做下载入口。
- 站内结果图既可能来自 `storageKey`，也可能来自公开路径 `/assets/...`；下载链路要同时兼容这两种来源。
- ZIP 下载优先作为稳定兜底路径；直接下载多张采用顺序触发策略，失败时给出非阻塞提示即可。
## 2026-03-24 费用与后台列表规则补充

- 本项目已废弃积分体系；运行时不再使用 `credits`、`creditCost` 或 `CreditLog`。
- 应用费用统一使用人民币分存储：`App.estimatedPriceFen`。
- 前端金额输入输出统一使用元字符串；接口层统一接收 `estimatedPriceYuan`，服务端负责校验并转换为分。
- 历史任务费用展示必须优先读取 `Task.estimatedPriceFenSnapshot`，不能直接回查当前应用价格。
- 应用列表、新建应用、编辑应用、任务提交前都应展示同一套预计费用语义：`预计费用 ¥x.xx`。
- 后台高频管理页优先采用“列表内直接编辑 + 即时保存”，不要强迫运营频繁进入详情页。
- 有 `sortOrder` 的后台列表默认应以 `sortOrder` 作为主排序字段，并支持在列表中直接修改。
- 列表直改失败时必须回滚本地临时值并给出明确提示，不能让错误状态残留在行内。
## 2026-03-24 提示词模板规则补充

- 提示词模板的服务端执行规则固定为：优先变量替换，缺失变量允许为空；只有在模板正文完全不含 `{{input.xxx}}` 时才回退到“模板正文 + 用户输入”的旧拼接逻辑。
- 当前约定的模板输入变量前缀固定为 `input`，首批标准变量为 `prompt`、`style`、`scene`、`negative_prompt`。
- `task.prompt` 继续只保存用户主输入，不把后台模板正文回写到用户可见任务记录里。
- 后台模板编辑体验优先按桌面端单屏工作台设计，不再回退到长滚动弹窗。
- 模板正文编辑区默认保持白底；变量插入和执行说明必须在编辑器附近显式展示，避免运营误解拼接规则。
- 提示词模板的标签与分类管理优先在当前编辑流内就地完成，避免把运营人员强制跳转到独立管理页。
- 提示词模板编辑器里的“封面”区域默认展示完整比例缩略图，不再用裁切式横幅预览掩盖主体。
- 提示词模板封面缩略图应直接提供查看完整图片的入口，避免运营为了确认细节离开当前编辑流。
## 2026-03-24 飞书字段映射长期规则补充

- 飞书同步字段分为两层：
  - 共享基础字段，供站点级“集成设置”使用
  - 应用参数字段，统一使用 `params.<fieldKey>` 键格式，只在应用编辑器中暴露当前应用自己的表单字段
- 客户端可用的飞书映射逻辑必须依赖纯共享模块，禁止直接引用带 `prisma` 或其他服务端依赖的同步服务模块。
- 全局映射只允许共享基础字段；应用级映射允许“共享基础字段 + 当前应用表单字段”，保存时必须按允许字段集合做服务端校验。
- `syncMappingJson` 的长期存储格式保持为对象：
  - `{ [sourceKey]: feishuColumnName }`
  - 其中 `sourceKey` 允许共享字段键和 `params.<fieldKey>`
- 任务同步到飞书时，`paramsJson` 需要展开为 `params.<fieldKey>`：
  - 字符串直接使用
  - 数组按换行拼接
  - 数字/布尔值转字符串
  - 空值不入表
## 2026-03-24 飞书模板映射规则补充

- 飞书共享字段除了任务、状态、应用名、提示词等基础字段外，还支持：
  - `promptTemplateName`
  - `promptTemplateContent`
- 提示词模板相关的飞书同步必须从任务快照读取，不能依赖提交当次的临时变量。
- 如果用户在提交任务时选择了提示词模板，任务结果快照里应保留：
  - `resultJson.promptTemplate.id`
  - `resultJson.promptTemplate.name`
  - `resultJson.promptTemplate.templatePrompt`
- `allInfo` 作为完整快照字段，也需要包含提示词模板快照，便于排查历史同步问题。
## 2026-03-24 RunningHub 多通道调度长期规则

- RunningHub 的并发能力归属在集成设置的 RunningHub 区块，不能再挂在飞书配置语义下。
- RunningHub 通道统一使用 `runninghubChannelsJson` 建模，字段固定为：`code / name / apiKeyEnvName / concurrencyLimit / priority / enabled`。
- RunningHub API Key 继续只走环境变量；后台只配置通道元数据，不存储明文密钥。
- 任务提交默认先写入本地 `QUEUED` 队列，再由调度器按优先级和并发占用派发；不能再回退成“创建任务时直接调用 RunningHub”。
- 通道选择规则固定为：仅选启用通道、优先级数字越小越先用、应用级限制优先于全局自动调度、所有通道都满时保持本地排队。
- 通道占用数只统计已经占用 provider 配额的 `RUNNING` 任务，并且任务上必须已有 `runninghubChannelCode`。
- `Task.runninghubChannelCode / runninghubChannelName` 属于任务快照字段，派发成功后必须写入，便于后续轮询、取消、同步和排障统一复用。
- `App.runninghubAllowedChannelCodesJson` 的 `null` 或空数组表示自动调度；非空数组表示只允许这些通道。
- 旧 `taskMaxConcurrency` 仅作为兼容读取来源，不再作为新功能配置入口；后台保存一次后应落成正式 `runninghubChannelsJson`。
## 2026-03-24 应用展示配置长期规则补充

- `showcaseImages` 是应用级“案例参考图片”配置，前台详情页统一展示为“效果展示”；数据层持久化字段为 `App.showcaseImagesJson`，运行时永远归一成字符串数组。
- 案例参考图片本轮只存 URL 和顺序，不增加标题、描述、跳转链接等扩展字段；上限固定为 100 张。
- 前台 `效果展示` 的回退顺序固定为：`showcaseImages[0]` -> `coverPoster` -> 默认占位图。
- `iconUrl / iconBgColor / authorName / authorAvatar / badgeLabel` 作为兼容字段暂时保留在数据库，但后台编辑器和前台展示不再继续使用。
## 2026-03-24 Vercel 部署长期规则补充

- 当前 `ai-workbench` 主仓库仍然是 `SQLite` 版本，因此只允许把 Vercel 自动部署脚本用于 `preview/demo`，不允许把 `file:...` 数据库直接发到 `production`。
- Vercel 半自动部署入口固定为：
  - `scripts/deploy-vercel.mjs`
  - `npm run deploy:vercel:preview`
  - `npm run deploy:vercel:production`
- 所有 Vercel 部署说明都必须同时写清楚：
  - 怎么用
  - 每个参数去哪里获取
  - 当前是否适合正式环境
- 如果仓库仍未完成 `Postgres + 对象存储` 迁移，就不能在对外说明里暗示“已经可以正式部署到 Vercel production”。
## 2026-03-25 默认登录账号长期规则补充

- 默认账号能力的真实来源固定在 `src/lib/default-login-accounts.ts`，不能再把账号密码分别散落到登录页文案、seed 路由和临时脚本里各自维护。
- 登录页不再公开展示默认账号密码；如果业务仍需要默认账号，只允许后端做运行时自修复或初始化。
- 默认账号运行时自修复只服务于固定保留账号：`admin`、`ops.a`、`design.c`，用于解决 Vercel / 新库未初始化或密码哈希漂移导致的登录失败。
- 工作台首页、系统概览、任务中心这类“总览页”默认要做服务端空态兜底；统计查询异常时优先保页面可打开，再单独排查数据库或外部依赖。
- `src/app/(workspace)` 路由组依赖登录态和数据库，默认应保持 `force-dynamic`，不要再让这组后台页参与构建期预渲染。
## 2026-03-24 任务中心长期规则补充

- 任务中心列表必须同时保留 `输入参考图` 与 `结果预览` 两个独立缩略图区；缩略图点击只负责放大预览，不承担打开任务详情的主交互。
- 任务中心里打开任务详情的主入口固定为 `站内编号` 与 `应用名`，默认走右侧抽屉；原 `/tasks/[id]` 页面继续作为深度详情页保留。
- 任务详情接口需要返回可供前端“人话化展示”的输入 schema 元数据，至少包含 `key / label / type / hidden`，避免后台直接暴露内部字段 key。
- 任务输入参考图排序规则固定优先级为：`sourceSlot` 明确标注的主图优先，其次按应用表单里的图片字段顺序回退；列表和详情抽屉必须共用同一套映射逻辑。
